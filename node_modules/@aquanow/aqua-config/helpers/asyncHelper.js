/**
 * @function sleep
 * @description delay function that would halt something for specified miliseconds
 * @param {number} ms miliseconds ie 1000 for 1 seconds
 */

function sleep(ms) {
  return new Promise(resolve => {
    setTimeout(() => resolve(), ms);
  });
}

module.exports = { sleep };
